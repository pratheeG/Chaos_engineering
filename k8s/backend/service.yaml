apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: chaos-ns
  labels:
    app: chaos-backend
spec:
  selector:
    app: chaos-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
